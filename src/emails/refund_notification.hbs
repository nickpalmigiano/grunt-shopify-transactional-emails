---
layout: branded.hbs
subject: Refund Notification
---
<table width="100%" cellpadding="0" cellspacing="0">
    {% if customer.name %}
    <tr>
        <td class="content-block"><h1>Dear \{{ customer.name }},</h1></td>
    </tr>
    {% endif %}
    <tr>
        <td class="content-block">\{{ shop_name }} has refunded your order (\{{ name }}){% if amount > 0 %} a total of \{{ amount | money_with_currency }}{% endif %}{% if refund_line_items.size > 0 %} for:{% else %}.{% endif %}</td>
    </tr>
    <tr>
        <td class="content-block">
            <table class="invoice">
                <tr>
                    <td>
                        <table class="invoice-items" cellpadding="0" cellspacing="0">
                           {% for line in refund_line_items %}
                            <tr>
                                <td>\{{ line.line_item.title }}</td>
                                <td class="alignright">x \{{ line.quantity }}</td>
                            </tr>
                           {% endfor %}
                        </table>
                    </td>
                </tr>
            </table>
        </td>
    </tr>
    <tr>
        <td class="content-block">The next time you shop at \{{ shop.name }}, you can save time at checkout by logging into your account. This will prefill your address information at the checkout.</td>
    </tr>
</table>

